version: "3.3"

services:
  webapp:
    image: web-app:latest
    restart: always
    container_name: web-app
    ports:
      - 3000:80
    working_dir: /app
    environment:
      - RECAPTCHA_SITE_KEY=$RECAPTCHA_SITE_KEY
